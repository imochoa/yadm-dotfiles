
[greenclip]
  blacklisted_applications = []
  enable_image_support = true
  history_file = "/home/imochoa/.cache/greenclip.history"
  # path without ending / will generate mktemp directory at the prefix location
  image_cache_directory = "/tmp/greenclip"
  max_history_length = 50
  max_selection_size_bytes = 0
  static_history = ["Greenclip has been updated to v4.1, update your new config file at ~/.config/greenclip.toml",'''¯\_(ツ)_/¯''']
  trim_space_from_selection = true
  use_primary_selection_as_input = false
